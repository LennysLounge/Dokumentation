# ACC-broadcasting-client-example
The Acc broadcasting client example hosted on github

This is the example broadcasting API client from the server folder.
There is no active development here. Only a host.

